# 2026-08-13 工作日志

## v1.2.0 迭代:资产剥离 + 按月×平台分目录交付

针对「所有数据堆在一个大文件/单库,交付体量随视频数膨胀」的设计问题完成重构。

### 背景与设计
- 用户提出:原始接口响应全文剥离为项目独立资产;数据按 年(主)/月(次) 分类,如 `2026_09_bilibili`;交付只给当月。
- 关键设计(月份双轨制):**交付数据按发布月份**归目录(视频完整档案),**原始资产按抓取月份**归档(当时快照)。

### 实施内容
- `sf_core.py`:
  - `api_archives` 加 `asset_path` 列(ALTER 兼容旧库);`add_api_archive` 改为落盘资产文件 + 存指针,body_json 不再入库
  - 新增 `write_asset`/`load_asset`/`asset_rel_path`(同名文件冲突时 URL hash 后缀防覆盖)
  - 新增 `export_monthly(year_month, platform, split_video, since)`:产出 `{YYYY_MM}_{platform}/` 目录
    (manifest.json 清单 + videos/snapshots/trends/audience),快照去重(最新 cumulative/T7 + 全部 deep,剔除 raw_json)
- `sf_push.py` 重写:`--month/--platform/--split-video/--since/--all/--to`,默认推主项目 snapshots/
- `xhs_audit.py` / `xhs_reparse.py`:改经 `asset_path` 读资产文件(load_asset)
- 新增 `sf_migrate_assets.py`(一次性迁移)
- 文档:VERSION_HISTORY.md(v1.2.0)、README.md、SF/README.md 已更新

### 结果
- 存量 890 条 body_json → 890 资产文件,抽查 20/20 一致,body_json 置空
- DB:22.7MB → 4.06MB(VACUUM,-82%);备份在 data/backup/sq_metrics_v1.1.0_20260813.db
- 验证:py_compile 全过;xhs_audit 211 篇跑通;xhs_reparse 单篇跑通;三平台按月导出/--split-video/--since 通过

### 注意
- export/ 下旧格式 sq_metrics_*.json/xlsx 保留为本地归档(推主项目只走新格式)
- 数据分布:2021-05 ~ 2026-08 共 63 个月×平台;当前交付目录:2025_09_bilibili、2026_07_xiaohongshu、2026_08_douyin(尚未正式推主项目)
